#pragma once
#include "node.h"

//treeNode* searchBST(treeNode* root, Sales x);
treeNode* insertBSTNode(treeNode* p, Sales x);
//void deleteBSTNode(treeNode* root, Sales key);

void displayInorder(treeNode* root);